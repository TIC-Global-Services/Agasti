import ContainerLayout from "@/layout/ContainerLayout";

export default function Home() {
  return (
    <ContainerLayout>
      Don't Use this ContainerLayout direcctly on page.tsx. Use it on component
      level.
    </ContainerLayout>
  );
}
